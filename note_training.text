
1: 
headless
non headless

-> Edit trong "playwright.config" - user



2.
* browser selection





* base on testcase

* parallel
=> Typescript
=> Tạo base example Playwright


Research: 
* Dùng cái gì để control browser

dùng browser

-> browser -> context -> page.goto ....



* Cách tương tác các control


  await page.goto('https://demo.automationtesting.in/Register.html');

  // Text input
  await page.fill('input[placeholder="First Name"]', 'Nguyen');

  // Radio
  await page.check('input[value="Male"]');

  // Checkbox
  await page.check('input[type="checkbox"][value="Movies"]');

  // Dropdown
  await page.selectOption('#Skills', 'Java');

  // Date picker
  await page.fill('#datepicker', '08/01/2025');

  // Upload file
  await page.setInputFiles('#imagesrc', 'tests/data/avatar.jpg');

  // Textarea
  await page.fill('textarea', 'Đây là phần mô tả dài...');

  // Click button
  await page.click('#submitbtn');



* Ưu nhược điểm so với các tool đã từng làm (Nhanh hay chậm như thế nào? Support như thế nào?)
* Typescript => Tạo example


* Bất đồng bộ (Async & Await)
-> async: đánh dấu function sẽ dùng await bên trong
-> await: để đảm bảo hành động được hoàn thành rồi mới thực hiện hành động tiếp theo.

* Data Object



| Tính chất         | Mục đích sử dụng trong test tự động hóa                          |
| ----------------- | ---------------------------------------------------------------- |
| `visible`         | Kiểm tra UI có đang hiển thị đúng, sẵn sàng để thao tác không    |
| `enabled`         | Kiểm tra chức năng có bị khoá không (ví dụ khi form chưa hợp lệ) |
| `editable`        | Kiểm tra trường input có cho nhập không                          |
| `stable`          | Đảm bảo click/hover/fill vào phần tử đang ổn định trên UI        |
| `receives events` | Tránh bị lớp khác che mất nút cần click                          |


Iframe
- await page.frameLocator('#singleframe').locator('input').fill('Playwright');









| Giá trị              | Ý nghĩa                                                                |
| -------------------- | ---------------------------------------------------------------------- |
| `"load"`             | Đợi đến khi sự kiện `load` của trang được kích hoạt (window\.onload)   |
| `"domcontentloaded"` | Đợi đến khi DOM được load xong (document.readyState === "interactive") |
| `"networkidle"`      | Đợi đến khi **không còn request mạng nào đang mở trong 500ms**         |
| `"commit"`           | Đợi khi điều hướng bắt đầu (ít được dùng)                              |

